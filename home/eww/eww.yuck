; ─────────────────────────────────────────────
;  VARIABLES
; ─────────────────────────────────────────────

(defpoll cpu-porcentaje
  :interval "3s"
  :initial "0"
  `top -bn1 | grep "Cpu(s)" | awk '{print int($2)}'`)

(defpoll ram-usado
  :interval "5s"
  :initial "0G"
  `free -h | awk '/^Mem/ {print $3}'`)

(defpoll ram-porcentaje
  :interval "5s"
  :initial "0"
  `free | awk '/^Mem/ {printf "%.0f", $3/$2*100}'`)

(defpoll disco-porcentaje
  :interval "30s"
  :initial "0"
  `df -h / | awk 'NR==2 {gsub("%",""); print $5}'`)

(defpoll disco-usado
  :interval "30s"
  :initial "0G / 0G"
  `df -h / | awk 'NR==2 {print $3 " / " $2}'`)

(defpoll musica-titulo
  :interval "2s"
  :initial ""
  `playerctl metadata title 2>/dev/null || echo ""`)

(defpoll musica-artista
  :interval "2s"
  :initial ""
  `playerctl metadata artist 2>/dev/null || echo ""`)

(defpoll musica-estado
  :interval "2s"
  :initial "Stopped"
  `playerctl status 2>/dev/null || echo "Stopped"`)

(defpoll musica-progreso
  :interval "2s"
  :initial "0"
  `bash ~/.config/eww/scripts/musica-progreso.sh`)

(defpoll pomo-estado
  :interval "1s"
  :initial `bash ~/.config/eww/scripts/pomodoro.sh status`
  `bash ~/.config/eww/scripts/pomodoro.sh tick; bash ~/.config/eww/scripts/pomodoro.sh status`)


; ─────────────────────────────────────────────
;  MÚSICA — siempre visible, se adapta al estado
; ─────────────────────────────────────────────

(defwidget widget-musica []
  (box
    :class "widget widget-musica"
    :orientation "v"
    :spacing 10
    ; Cabecera: carátula + info
    (box
      :orientation "h"
      :spacing 12
      :valign "center"
      (box
        :class "musica-caratula"
        :width 48
        :height 48
        (label :class "musica-caratula-icono" :text "♪"))
      (box
        :orientation "v"
        :spacing 2
        :valign "center"
        :hexpand true
        (label
          :class "musica-titulo"
          :text {musica-titulo != "" ? musica-titulo : "Sin reproducción"}
          :truncate true
          :xalign 0)
        (label
          :class "musica-artista"
          :text {musica-artista != "" ? musica-artista : "—"}
          :truncate true
          :xalign 0)))
    ; Barra de progreso — oculta cuando no hay reproducción
    (scale
      :class "musica-progreso"
      :value {musica-estado != "Stopped" ? musica-progreso : 0}
      :min 0
      :max 100
      :draw-value false)
    ; Controles
    (box
      :class "musica-controles"
      :orientation "h"
      :halign "center"
      :spacing 20
      (button
        :class "ctrl-btn"
        :onclick "playerctl previous"
        (label :text "󰒮"))
      (button
        :class "ctrl-btn ctrl-play"
        :onclick "playerctl play-pause"
        (label :text {musica-estado == "Playing" ? "󰏤" : "󰐊"}))
      (button
        :class "ctrl-btn"
        :onclick "playerctl next"
        (label :text "󰒭")))))


; ─────────────────────────────────────────────
;  POMODORO
; ─────────────────────────────────────────────

(defwidget widget-pomodoro []
  (box
    :class "widget widget-pomodoro"
    :orientation "v"
    :spacing 10
    ; Cabecera — mismo estilo que sysmon-title
    (box
      :orientation "h"
      :spacing 0
      (label
        :class "pomo-cabecera-label"
        :text {pomo-estado.mode == "work" ? "FOCO" : "DESCANSO"}
        :xalign 0
        :hexpand true)
      (label
        :class "pomo-sesiones"
        :text "${pomo-estado.sessions} 󰔟"))
    ; Tiempo grande
    (label
      :class "pomo-tiempo"
      :text {pomo-estado.time}
      :halign "center")
    ; Barra progreso
    (scale
      :class "pomo-barra"
      :value {pomo-estado.progress}
      :min 0
      :max 100
      :draw-value false)
    ; Controles
    (box
      :class "pomo-controles"
      :orientation "h"
      :halign "center"
      :spacing 4
      (button
        :class "pomo-btn"
        :onclick "bash ~/.config/eww/scripts/pomodoro.sh reset"
        (label :text "󰑐"))
      (button
        :class {pomo-estado.status == "running" ? "pomo-btn pomo-btn-activo" : "pomo-btn pomo-btn-play"}
        :onclick "bash ~/.config/eww/scripts/pomodoro.sh pause"
        (label :text {pomo-estado.status == "running" ? "󰏤" : "󰐊"}))
      (button
        :class "pomo-btn"
        :onclick "bash ~/.config/eww/scripts/pomodoro.sh skip"
        (label :text "󰒭")))))


; ─────────────────────────────────────────────
;  SISTEMA
; ─────────────────────────────────────────────

(defwidget widget-sistema []
  (box
    :class "widget widget-sistema"
    :orientation "v"
    :spacing 12
    (label :class "sistema-titulo" :text "SISTEMA" :xalign 0)
    ; CPU
    (box :orientation "v" :spacing 4
      (box :orientation "h"
        (label :class "metrica-etiqueta" :text "CPU" :xalign 0 :hexpand true)
        (label :class "metrica-valor" :text "${cpu-porcentaje}%"))
      (scale
        :class "metrica-barra metrica-barra-cpu"
        :value cpu-porcentaje
        :min 0 :max 100
        :draw-value false))
    ; RAM
    (box :orientation "v" :spacing 4
      (box :orientation "h"
        (label :class "metrica-etiqueta" :text "RAM" :xalign 0 :hexpand true)
        (label :class "metrica-valor" :text ram-usado))
      (scale
        :class "metrica-barra metrica-barra-ram"
        :value ram-porcentaje
        :min 0 :max 100
        :draw-value false))
    ; SSD
    (box :orientation "v" :spacing 4
      (box :orientation "h"
        (label :class "metrica-etiqueta" :text "SSD" :xalign 0 :hexpand true)
        (label :class "metrica-valor" :text disco-usado))
      (scale
        :class "metrica-barra metrica-barra-ssd"
        :value disco-porcentaje
        :min 0 :max 100
        :draw-value false))))


; ─────────────────────────────────────────────
;  PANEL + WINDOW
; ─────────────────────────────────────────────

(defwidget panel-escritorio []
  (box
    :class "panel-escritorio"
    :orientation "v"
    :spacing 10
    :valign "start"
    (widget-musica)
    (widget-pomodoro)
    (widget-sistema)))

(defwindow escritorio
  :monitor 0
  :geometry (geometry
    :x "8px"
    :y "64px"
    :width "220px"
    :anchor "top right")
  :stacking "overlay"
  :exclusive false
  :focusable false
  :namespace "eww-escritorio"
  (panel-escritorio))
